<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>io.github.cheneyzhang93</groupId>
    <artifactId>bellringer</artifactId>
    <version>0.1.0-SNAPSHOT</version>
  </parent>

  <artifactId>bellringer-protocol</artifactId>
  <name>bellringer-protocol</name>
  <description>跨仓事件契约：AlertEvent v1、枚举与传输信封（纯 Java 8，零框架依赖）</description>

  <dependencies>
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
    <!-- 仅测试用：用与两端一致的默认 Jackson 映射跑契约 fixture 测试；主构件保持零依赖 -->
    <dependency>
      <groupId>com.fasterxml.jackson.core</groupId>
      <artifactId>jackson-databind</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>
</project>
